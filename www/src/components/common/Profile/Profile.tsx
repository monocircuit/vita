import React from "react";

interface Props {}

const Profile = (props: Props) => {
  return (
    <div
      id="profile"
      className="size-full aspect-square rounded-full border-solid border-secondary border-(length:--stroke)"
    ></div>
  );
};

export default Profile;
