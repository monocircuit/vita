import { LinearChronicle } from "@/utils/schemas/Chronicle";

function inferChroniclePath(
  chronicle: LinearChronicle,
  chronicles: LinearChronicle[],
): { layer: number; date: number }[] {
  return [];
}
